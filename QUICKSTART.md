# 🚀 Швидкий старт

## 📋 Що вже зроблено

✅ **Повнофункціональний сайт гадання на таро з AI**

## 🎯 Як користуватися

### Сервери вже запущені:
- **Backend**: http://localhost:5000
- **Frontend**: http://localhost:5174

### Що працює:
1. Відкрийте http://localhost:5174/ у браузері
2. Введіть питання (опціонально)
3. Натисніть "Почати гадання" - отримаєте 3 випадкові карти
4. Натисніть "Отримати інтерпретацію AI" - отримаєте детальну інтерпретацію від AI
5. Перемикайте мову кнопкою EN/UK

## ⚠️ ВАЖЛИВО: Налаштування Google Gemini API

**Без цього AI інтерпретація не працюватиме!**

### Крок 1: Отримайте API ключ
1. Перейдіть: https://ai.google.dev/gemini-api/docs/api-key
2. Натисніть "Get API key"
3. Створіть новий проект або виберіть існуючий
4. Натисніть "Create API key"
5. **Скопіюйте ключ** (він виглядає як: `AIzaSy...`)

### Крок 2: Додайте ключ у проект
1. Відкрийте файл: `server/.env`
2. Знайдіть рядок: `GEMINI_API_KEY=your_gemini_api_key_here`
3. Замініть `your_gemini_api_key_here` на ваш ключ
4. Збережіть файл

### Крок 3: Перезапустіть сервер
```bash
# Закрийте попередній сервер (Ctrl+C у терміналі)
cd server
npm run dev
```

## 🎨 Функції

### Реалізовано:
- ✅ Вибір 3 випадкових карт таро
- ✅ Відображення карт з позиціями (Минуле/Теперішнє/Майбутнє)
- ✅ Інформація про кожну карту (назва, значення)
- ✅ AI інтерпретація через Google Gemini
- ✅ Поле для введення питання
- ✅ Багатомовність (українська/англійська)
- ✅ Містичний дизайн з анімаціями
- ✅ Responsive design

### Можна додати:
- 📝 Автентифікація користувачів
- 📚 Історія гадань
- 🃏 Інтерактивний вибір карт з колоди
- 🎭 Додаткові анімації
- 🔊 Звукові ефекти

## 🛠 Команди

### Запуск серверів (якщо зупинилися):

**Backend:**
```bash
cd server
npm run dev
```

**Frontend:**
```bash
cd client
npm run dev
```

### Корисні команди:

**Перегляд бази даних:**
```bash
cd server
npx prisma studio
# Відкриється на http://localhost:5555
```

**Тестування API:**
```bash
# Перевірка здоров'я сервера
curl http://localhost:5000/health

# Отримання випадкових карт
curl http://localhost:5000/api/readings/cards/random

# Список всіх 78 карт
curl http://localhost:5000/api/readings/cards
```

## 📚 API Endpoints

### Публічні (не потрібна авторизація):
- `GET /api/readings/cards` - Всі 78 карт таро
- `GET /api/readings/cards/random` - Випадкові 3 карти
- `POST /api/readings/interpret` - AI інтерпретація

### З авторизацією (потребують JWT токен):
- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/login` - Вхід
- `GET /api/readings/history` - Історія гадань
- `DELETE /api/readings/:id` - Видалити гадання

## 🐛 Виправлення проблем

### AI інтерпретація не працює:
1. Перевірте, що ви додали GEMINI_API_KEY у `server/.env`
2. Перезапустіть сервер: `cd server && npm run dev`
3. Перевірте консоль сервера на помилки

### Сторінка не завантажується:
1. Перевірте, що обидва сервери запущені
2. Backend: http://localhost:5000/health повинен відповідати
3. Frontend: http://localhost:5174/ повинна відкриватися

### Порт зайнятий:
Якщо порт 5174 зайнятий, Vite автоматично вибере інший (наприклад, 5175).
Подивіться у консолі, на якому порту запустився frontend.

## 📖 Структура проекту

```
tarot-reading/
├── server/              # Backend (Node.js + Express)
│   ├── src/
│   │   ├── services/   # aiService.js - тут Gemini API
│   │   ├── routes/     # API endpoints
│   │   └── utils/      # tarotData.js - 78 карт
│   ├── prisma/         # База даних
│   └── .env           # ← ДОДАЙТЕ СЮДИ API KEY
│
├── client/             # Frontend (React)
│   ├── src/
│   │   ├── App.tsx    # Головний компонент
│   │   └── App.css    # Стилі
│   └── .env
│
└── README.md          # Повна документація
```

## 🎓 Наступні кроки

1. **Спробуйте сайт**: http://localhost:5174/
2. **Додайте Gemini API key** для AI інтерпретації
3. **Перегляньте код** у `client/src/App.tsx`
4. **Додайте нові функції** за бажанням

## 💡 Приклад використання

1. Відкрийте http://localhost:5174/
2. Введіть питання: "Що мене чекає в майбутньому?"
3. Натисніть "Почати гадання"
4. Побачите 3 карти з описами
5. Натисніть "Отримати інтерпретацію AI"
6. Отримаєте детальну інтерпретацію українською/англійською

---

**Створено за допомогою Claude Code 💜**
