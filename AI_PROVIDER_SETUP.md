# Налаштування AI провайдера для інтерпретації карт Таро

Ваш сайт підтримує **два AI провайдери** для генерації інтерпретацій:

## 🆚 Порівняння провайдерів

| Параметр | Google Gemini 2.5 Flash | OpenAI GPT-4o-mini |
|----------|------------------------|-------------------|
| **Ціна** | ✅ БЕЗКОШТОВНО | 💰 Платно (~$0.0003 за інтерпретацію) |
| **Якість** | ⭐⭐⭐⭐ Дуже добре | ⭐⭐⭐⭐⭐ Відмінно |
| **Швидкість** | ⚡ Дуже швидко (1-2 сек) | ⚡ Швидко (2-4 сек) |
| **Ліміти** | 15 запитів/хв, 1M токенів/день | Залежить від балансу |
| **Реєстрація** | Google акаунт | Потрібна картка |

## ✅ Рекомендація

**Для початку використовуйте Gemini** - він безкоштовний і дає чудові результати!

Якщо потрібна максимальна якість - перейдіть на OpenAI.

## 🔧 Як налаштувати

### Варіант 1: Google Gemini (рекомендовано для початку)

1. Отримайте безкоштовний API ключ за інструкцією: [HOW_TO_GET_GEMINI_KEY.md](HOW_TO_GET_GEMINI_KEY.md)

2. Відкрийте `server/.env` і додайте:
   ```env
   GEMINI_API_KEY=AIzaSyВашКлюч...
   AI_PROVIDER=gemini
   ```

3. Перезапустіть сервер:
   ```bash
   cd server
   npm start
   ```

4. Готово! 🎉

### Варіант 2: OpenAI ChatGPT (максимальна якість)

1. Отримайте OpenAI API ключ за інструкцією: [HOW_TO_GET_OPENAI_KEY.md](HOW_TO_GET_OPENAI_KEY.md)

2. Відкрийте `server/.env` і додайте:
   ```env
   OPENAI_API_KEY=sk-proj-ВашКлюч...
   AI_PROVIDER=openai
   ```

3. Перезапустіть сервер:
   ```bash
   cd server
   npm start
   ```

4. Готово! 🎉

## 🔄 Перемикання між провайдерами

Ви можете в будь-який момент змінити провайдера:

1. Відкрийте `server/.env`
2. Змініть рядок `AI_PROVIDER=`:
   - `AI_PROVIDER=gemini` - для Google Gemini
   - `AI_PROVIDER=openai` - для OpenAI ChatGPT
3. Перезапустіть сервер

**Приклад файлу `.env` з обома ключами:**
```env
PORT=5000
DATABASE_URL="file:./dev.db"
JWT_SECRET=tarot_reading_super_secret_key_change_in_production
GEMINI_API_KEY=AIzaSyВашGeminiКлюч...
OPENAI_API_KEY=sk-proj-ВашOpenAIКлюч...
AI_PROVIDER=gemini
NODE_ENV=development
```

## ❓ Що вибрати?

### Виберіть Gemini якщо:
- ✅ Ви хочете безкоштовне рішення
- ✅ Вам достатньо дуже хорошої якості
- ✅ Ви робите особистий проект або тестуєте
- ✅ Робите багато запитів (15/хвилину безкоштовно)

### Виберіть OpenAI якщо:
- ✅ Вам потрібна максимальна якість відповідей
- ✅ Ви готові платити ~$0.0003 за інтерпретацію
- ✅ Ви робите комерційний проект
- ✅ У вас вже є OpenAI акаунт з кредитами

## 🆘 Проблеми?

### "AI provider not configured"
- Перевірте що ви додали відповідний API ключ в `.env`
- Перевірте що назва ключа правильна: `GEMINI_API_KEY` або `OPENAI_API_KEY`

### "Invalid API key"
- Для Gemini: ключ повинен починатись з `AIzaSy`
- Для OpenAI: ключ повинен починатись з `sk-`
- Переконайтесь що немає пробілів до або після ключа

### Gemini повертає помилку 404
- Ваш Gemini ключ застарілий!
- Створіть новий ключ на https://aistudio.google.com/app/apikey
- Старі ключі (до 2024) не працюють з Gemini 2.5

### OpenAI повертає помилку "Insufficient quota"
- Додайте кредити в OpenAI Billing
- Або перейдіть на безкоштовний Gemini

---

**Детальні інструкції:**
- Google Gemini: [HOW_TO_GET_GEMINI_KEY.md](HOW_TO_GET_GEMINI_KEY.md)
- OpenAI: [HOW_TO_GET_OPENAI_KEY.md](HOW_TO_GET_OPENAI_KEY.md)
